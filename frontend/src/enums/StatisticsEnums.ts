export enum SortByEnum {
  totalPoints = 'Total points',
  highestStreak = 'Highest streak',
  currentStreak = 'Current streak',
  totalExercises = 'Total exercises',
}

export enum ExerciseStatsFilterEnum {
  allTime = 'All time',
  last30Days = 'Last 30 days'
}
